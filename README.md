# GPSsurface

Compose map / path sketch: plot points on a surface, inspect path details, ViewModels for selected points and screen coordinates.

## Stack

Kotlin, Jetpack Compose.

## Run

Open the project in Android Studio and run the `app` configuration.

```bash
./gradlew :app:assembleDebug
```
